module.exports = function (app) {
    const auth = require('../middleware/auth');
    const advertController = require('../controllers/reserveController');


//======================== Advert ==========================//
    
    // app.route('/')
    //     .post(reserveController.);

}