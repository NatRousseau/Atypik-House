module.exports = function (app) {
    const auth = require('../middleware/auth');
    const activityController = require('../controllers/advertController');


//======================== Activity ==========================//

    // app.route('/createActivity')
    //     .post(auth,activityController.createActivity);

}